class Solution {
    public boolean isPalindrome(int x) {
        xString = x.toString();
        start = 0
        end = xString.length - 1
        while (start < end) {
            list = New Arraylist()
            for (i = 0, i < list.length, i++ ) {
                list.append[i] =
            }
        }
    }

    public isPalindromeR(int x) {
        xString = x.toString();
        start = 0
        end = xString.length - 1
        return check(x, start, end)
    }

    public function check(x, start, end) {
        if (x[start] < x[end]) {
            for (i = 0, i < )
        }
    }

    // Example n = 5 (從 1 + 2 + 3 + .... + 5)
    public function sum(n) {
        if (n == 1) {
            return 1
        }
        // 如果 n 是 1 的話則回傳自己

    }
}